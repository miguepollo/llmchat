[package]
name = "llmchat"
version = "0.1.0"
edition = "2021"
description = "Cliente de chat ligero y nativo para APIs OpenAI-compatible"
license = "MIT"

[dependencies]
eframe = "0.32"
reqwest = { version = "0.12", features = ["json", "stream"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time", "net"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
pulldown-cmark = "0.12"
futures-util = "0.3"
rfd = "0.15"
image = "0.25"
lopdf = "0.34"
zip = "2"
base64 = "0.22"
flate2 = "1"

[profile.release]
opt-level = 3
lto = "thin"
strip = true
